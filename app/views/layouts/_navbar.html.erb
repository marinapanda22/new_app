<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->

    <div class="navbar-header">
      <%= link_to 'Галлерея', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">

        <% if signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" id="login_dropdown" role="button" aria-haspopup="true" aria-expanded="false">Аккаунт <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "Личный кабинет", profile_path(current_user.id) %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Выйти", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>

            <li><%= link_to "Панель управления", admin_root_path if current_user.role?(:admin) %></li>
        <% else %>
            <li><%= link_to "Регистрация", new_user_registration_path %></li>

                <li id="login"><%= link_to "Вход", new_user_session_path %></li>
              </ul>
            </li>
        <% end %>
      </ul><!-- /.login toolbar -->
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>